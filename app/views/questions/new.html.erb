
<%= form_with scope: :question, url: user_questions_path(user_id: current_user.id), local: true do |form| %>

  <p>
    <%= form.label :question %><br>
    <%= form.text_area :question %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', user_questions_path %>